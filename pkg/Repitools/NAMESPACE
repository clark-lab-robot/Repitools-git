#.onLoad <- function(lib, pkg) require(methods)

#  All functions exported other than those starting with "."
import(IRanges, BSgenome, methods)
exportClasses(FastQC, SequenceQC, SequenceQCSet)
exportMethods(show)
exportPattern("^[^\\.]")
